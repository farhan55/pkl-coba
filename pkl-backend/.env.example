# MongoDB Atlas Connection String
# Ganti dengan connection string dari MongoDB Atlas Anda
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/pkl-db?retryWrites=true&w=majority

# JWT Secret Key - GANTI dengan string random yang kuat
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Port (Render akan set otomatis, default 5000 untuk local)
PORT=5000

# Frontend URL untuk CORS (ganti dengan URL Vercel Anda setelah deploy)
FRONTEND_URL=http://localhost:5173
